[build]
command = "php artisan optimize && npm run build"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"

[database]
type = "postgresql"
connect = false